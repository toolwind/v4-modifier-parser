@toolwind/v4-modifier-parser re-exports Tailwind CSS v4’s `parseModifier` as `parseModifierV4`.

Use this in plugins to ensure the same modifier parsing across Tailwind CSS v3 and v4 when using `modifiers: 'any'`.